// Explicit type annotation
let company: string = "Axis Communications"

// Implicit type annotation
let country = "Sweden"

// String
let firstName = "John"
let surname = "Doe"
let name = `${firstName} ${surname}`

// Number
let age = 42
let pi = 3.14159265358979323846

// Array
let fruits = ["apple", "orange", "pear"]

// Object
let car = {
    brand: "Tesla",
    model: "Model S",
    color: "red"
}

// Enum
enum Direction {
    North,
    East,
    South,
    West
}

// Types
type angle = 0 | 90 | 180 | 270

export {}
